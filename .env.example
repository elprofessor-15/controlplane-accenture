# Optional API keys. Leave blank to use the built-in deterministic fallback.
GROQ_API_KEY=
OPENROUTER_API_KEY=
OPENAI_API_KEY=

# Default hosted model used when a provider key is configured.
DEFAULT_MODEL=llama-3.3-70b-versatile